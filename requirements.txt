# Pinned runtime environment, captured from a resolved install on
# Python 3.11 / Windows. Regenerate with `pip freeze` after upgrading.
#
# For the loose ranges used when installing the package itself, see
# [project.dependencies] in pyproject.toml. Dev and notebook tooling lives in
# the `dev` and `notebook` extras, not here.

# ── Core numerics & tabular ──────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.6
scikit-learn==1.9.1

# ── Imaging ──────────────────────────────────────────────────────────────
pillow==12.3.0
opencv-python==5.0.0.93

# ── Deep learning ────────────────────────────────────────────────────────
# On Linux these wheels bundle CUDA and are very large. For a CPU-only box:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0
torchvision==0.29.0
timm==1.0.29

# ── Configuration & CLI ──────────────────────────────────────────────────
pydantic==2.13.5
pydantic-settings==2.15.0
PyYAML==6.0.3
python-dotenv==1.2.3
typer==0.27.2

# ── Visualisation & progress ─────────────────────────────────────────────
matplotlib==3.11.2
seaborn==0.13.2
tqdm==4.70.1

# ── Notebook environment ─────────────────────────────────────────────────
jupyter==1.1.1
ipykernel==7.3.0
